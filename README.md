# kiwiOS

**A minimal, Arch-based fork of the pearOS project.**

## How to Build
1. Install `archiso`.
2. Clone this repo.
3. Edit `packages.x86_64` to add or remove software.
4. Run: `sudo mkarchiso -v -w ./work -o ./out .`
